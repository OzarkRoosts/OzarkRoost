<% const { adventures, categories } = require('../lib/adventure-directory'); const { getBundle } = require('../lib/affiliate-links'); const adventureBundle = getBundle('adventure'); const featured = adventures.filter(a => a.featured).slice(0, 6); const firstAffiliate = adventureBundle[0]; %>
<%- include('adventures-body', { adventures, categories, adventureBundle, featured, firstAffiliate }) %>